'use strict';

var gulp = global.gulp = global.gulp || require('gulp');
var thirdParty = global.thirdParty = global.thirdParty || require('./third-party.js');

gulp.task('react-publish', function (done) {
    thirdParty.publish('react', 'npm install &&  gulp build  && gulp ci-compile && ' +
        'gulp es-scripts && gulp esm5-scripts esm2015-scripts umd-scripts &&' +
        'gulp add-pure-webpack rm-temp && gulp license && gulp react-api', done);
});
