'use strict';
exports.config = {
    allScriptsTimeout: 300000,
    framework: 'jasmine',
    specs: ['spec.js'],
    capabilities: {
        browserName: 'chrome',
        chromeOptions: {
            args: ['--headless', '--disable-gpu', '--window-size=800,600', '--no-sandbox']
        }
    },
    jasmineNodeOpts: {
        showColors: true,
        defaultTimeoutInterval: 300000,
        print: function(err) { console.log(err);}
    },
};