using System;
using System.IO;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
#if !EJ2_DNX
using Microsoft.AspNetCore.Html;
#else
using System.Web;
#endif

namespace Syncfusion.EJ2.{{BaseClass}}
{
    public class {{className}}Builder : ControlBuilder
    {
        private {{className}} model = new {{className}}();

        public {{className}}Builder() { }

        public {{className}}Builder({{className}} model)
        {
            this.model = model;
        }
        {{mvcTemplate}}
        {{builderProperties}}

        public HtmlString Render()
        {
            ScriptRenderer.Add(model.GetScriptString());{{htmlString}}
            return new HtmlString("{{defaultTag}}");
        }

         public override TextWriter Output
        {
            get;
            set;
        }

        public string ID
        {
            get { return model.Id; }
            set { model.Id = value; }
        }
    }
}
