#if !EJ2_DNX
using Microsoft.AspNetCore.Razor.TagHelpers;
#endif
using Newtonsoft.Json;
using System;
using System.Collections.Generic;
using System.ComponentModel;
using System.Text;
using Newtonsoft.Json.Converters;

namespace Syncfusion.EJ2.{{BaseClass}}
{
    #if !EJ2_DNX
    [HtmlTargetElement("{{complexTagName}}", ParentTag = "{{collectionTagName}}"){{RestrictChildren}}]
    #endif
    [JsonObject(MemberSerialization.OptIn)]
    public class {{complexClassName}} : EJTagHelper
    {
        protected override bool IsChild { get { return true; } }

{{properties}}
    // appendProperrties
    }

    [HtmlTargetElement("{{collectionTagName}}", ParentTag = "{{parentTagName}}")]
    public class {{collectionClassName}} : EJTagHelper
    {
        protected override bool IsCollection { get { return true; } }

        protected override object GetList() {

            return new List<{{complexClassName}}>();
        }

        protected override string ParentPropertyName { get { return "{{parentPropertyName}}"; } }

    }

}
