#if !EJ2_DNX
using Microsoft.AspNetCore.Razor.TagHelpers;
#endif
using Newtonsoft.Json;
using System;
using System.Collections.Generic;
using System.ComponentModel;
using System.Text;
using Newtonsoft.Json.Converters;

namespace Syncfusion.EJ2.{{BaseClass}}
{
    #if !EJ2_DNX
    [HtmlTargetElement({{Selector}} ParentTag = "{{collectionTagName}}"){{RestrictChildren}}]
    #endif
    public class {{complexClassName}} : EJTagHelper
    {
        protected override bool IsComplex { get { return true; } }
       
{{Properties}}
        protected override string ParentPropertyName { get { return "{{ParentPropertyName}}"; } }
    }
}