#if !EJ2_DNX
using Microsoft.AspNetCore.Razor.TagHelpers;
using Microsoft.AspNetCore.Mvc.Formatters.Json;
#endif
using System.Collections.Generic;
using Newtonsoft.Json;
using System.Text;
using System.ComponentModel;
using Newtonsoft.Json.Converters;

namespace Syncfusion.EJ2.{{BaseClass}}
{
    #if !EJ2_DNX
    [HtmlTargetElement("{{elementName}}"){{RestrictChildren}}]
    #endif
    [JsonObject(MemberSerialization.OptIn)]
    public class {{className}} : EJTagHelper
    {
        protected override string TagName { get { return "{{tagName}}"; } }

        protected override string NameSpace { get { return "{{nameSpace}}"; } }

        protected override string ClassName { get { return "{{className}}"; } }
        
{{properties}}
    }
}