using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using Syncfusion.EJ2.{{BaseClass}};

namespace Syncfusion.EJ2
{
    public partial class EJ2
    {
        {{comment}}
        public {{className}}Builder {{className}}(String id)
        {
            var {{obj}} = new {{className}}Builder();
            {{obj}}.ID = id;
            {{obj}}.Output = this.Output;
            {{obj}}.Context = this.Context;
            return {{obj}};           
        }
    }
}